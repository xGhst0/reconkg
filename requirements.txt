# Runtime dependencies -- must agree with [project].dependencies in
# pyproject.toml. tests/test_packaging.py fails the build if they drift.
fastapi>=0.141.1
uvicorn>=0.52.3
pydantic>=2.13.4
defusedxml>=0.7.1
